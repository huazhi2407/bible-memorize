# 使用原版後端（SQLite，不需要 Firebase）

## ✅ 可以繼續使用原版後端

您的項目有兩個後端版本：
- **`server/`** - 原版（SQLite，本地文件系統）
- **`server-firebase/`** - Firebase 版本（需要 Firebase 配置）

## 🚀 快速開始（使用原版）

### 1. 安裝依賴

```bash
# 在項目根目錄
npm run install:all
```

### 2. 啟動開發服務器

```bash
# 同時啟動後端和前端
npm run dev
```

或者分別啟動：

```bash
# 終端 1：啟動後端
npm run dev:server

# 終端 2：啟動前端
npm run dev:client
```

### 3. 訪問應用

- 前端：http://localhost:5173
- 後端：http://localhost:3001

## 📋 原版後端的特點

### ✅ 優點
- **不需要 Firebase 配置**：直接使用 SQLite 數據庫
- **本地存儲**：所有數據存儲在本地
- **簡單設置**：無需配置 Firebase 項目
- **離線工作**：完全離線可用

### ⚠️ 限制
- **本地存儲**：數據只存在本地電腦
- **無法雲端同步**：無法在多設備間同步
- **部署限制**：部署到 Render 等服務時，免費方案重啟後數據會重置

## 🔄 切換回原版後端

如果您之前在使用 Firebase 版本，現在想切換回原版：

1. **確保使用原版後端**
   - 根目錄的 `package.json` 已經配置為使用 `server/` 目錄
   - `npm run dev` 和 `npm start` 都會使用原版後端

2. **檢查環境變量**
   - 原版後端不需要 Firebase 相關的環境變量
   - 只需要（可選）：
     - `ADMIN_PASSWORD`：管理員密碼（默認：admin123）
     - `JWT_SECRET`：JWT 密鑰（建議設置）

3. **啟動服務器**
   ```bash
   npm run dev
   ```

## 📁 項目結構

```
bible-memorize/
├── server/              ← 原版後端（SQLite）
│   ├── index.js
│   ├── db/             ← SQLite 數據庫
│   └── storage/        ← 錄音文件存儲
├── server-firebase/     ← Firebase 版本（可選）
├── client/             ← 前端（React）
└── package.json        ← 使用 server/ 目錄
```

## 🎯 使用場景

### 使用原版後端（SQLite）適合：
- ✅ 本地開發和測試
- ✅ 單機使用
- ✅ 不需要雲端同步
- ✅ 簡單部署（使用付費方案或外接數據庫）

### 使用 Firebase 版本適合：
- ✅ 需要雲端同步
- ✅ 多設備訪問
- ✅ 需要自動擴展
- ✅ 需要 Firebase 的其他功能

## 🧪 測試原版後端

```bash
# 1. 安裝依賴
npm run install:all

# 2. 啟動服務器
npm run dev

# 3. 訪問 http://localhost:5173
# 4. 使用預設管理員登入：
#    帳號：admin
#    密碼：admin123
```

## 📝 注意事項

1. **數據存儲位置**：
   - SQLite 數據庫：`server/data/`
   - 錄音文件：`server/storage/`

2. **預設管理員**：
   - 帳號：`admin`
   - 密碼：`admin123`（或環境變量 `ADMIN_PASSWORD`）

3. **部署到 Render**：
   - 免費方案重啟後數據會重置
   - 建議使用付費方案或外接數據庫

## 🔗 相關文檔

- `README.md` - 項目主文檔
- `server/` - 原版後端代碼
- `server-firebase/` - Firebase 版本（可選）
